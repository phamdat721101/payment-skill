# ─── Dependencies ────────────────────────────────────────────────────────────
node_modules/
.pnp/
.pnp.*
jspm_packages/
docs/

# ─── Build outputs ───────────────────────────────────────────────────────────
dist/
build/
out/
.tsup/
.turbo/
.next/
.cache/
.parcel-cache/

# TypeScript incremental cache
*.tsbuildinfo
.tsbuildinfo

# Lint / formatter caches
.eslintcache
.stylelintcache
.prettiercache

# ─── Pack & release artifacts ────────────────────────────────────────────────
*.tgz
*.tar.gz
*.zip

# ─── Test & coverage ─────────────────────────────────────────────────────────
coverage/
.nyc_output/
.vitest/
.test-results/
junit.xml

# ─── Logs ────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-debug.log
.npm/
.yarn/

# ─── Environment files (secrets) ─────────────────────────────────────────────
.env
.env.*
# Allow committing the example/template
!.env.example
!.env.template
!.env.sample

# ─── Wallet & key material — NEVER commit ────────────────────────────────────
*.wallet.json
*.keystore
*-keystore.json
*.key
*.pem
*.p12
*.pfx
*.priv
*.crt
private-key*
private_key*
secret*.json
mnemonic*
seed-phrase*
.n-payment/
.n-payment-test/
wallets/

# ─── Cloud / service credentials ─────────────────────────────────────────────
.aws/
.azure/
.gcp/
credentials.json
service-account*.json
gcp-credentials*.json
firebase-credentials*.json

# Generic credential extensions (not covered by .env / wallet sections above)
*.token
*-secrets.json
*.creds

# ─── n-payment-skill host installer outputs (local dev only) ────────────────
# Produced by `n-payment-skill install --target {cursor,windsurf,continue,copilot}`
# when the skill is exercised against its OWN source tree. These belong in
# consumer repos, never in this skill's repo. Root-anchored so a consumer's
# legitimate .github/ workflows are NOT affected when this file is reused.
/.continuerules
/.windsurfrules
/.cursor/
/.github/copilot-instructions.md

# ─── IDE / editor ────────────────────────────────────────────────────────────
.idea/
.vscode/
.zed/
.history/
*.swp
*.swo
*~
.\#*

# ─── OS noise ────────────────────────────────────────────────────────────────
.DS_Store
._*
.AppleDouble
Thumbs.db
desktop.ini
ehthumbs.db

# ─── Temporary / scratch ─────────────────────────────────────────────────────
tmp/
.tmp/
*.bak
*.tmp
*.orig
